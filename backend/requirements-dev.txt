# Test and development dependencies. Not installed in the runtime image.
# Install alongside the runtime set:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt
pytest==7.4.3
pytest-asyncio==0.21.1
